{
  "name": "pgpm-boilerplates",
  "version": "1.0.0",
  "description": "Boilerplate templates with placeholder detection",
  "scripts": {
    "find-placeholders": "ts-node scripts/find-placeholders.ts",
    "find-placeholders:default": "ts-node scripts/find-placeholders.ts ./default",
    "update-deps": "ts-node scripts/update-deps.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
